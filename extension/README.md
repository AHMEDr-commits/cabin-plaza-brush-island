# Loreweave Chrome extension

Manifest V3. Load this folder unpacked.

Keys stay in `chrome.storage.local` and are sent only to the provider base URL you configure.

Default shortcut: Ctrl+Shift+Space (open). Ctrl+Shift+K (command prompt). Rebind in `chrome://extensions/shortcuts`.
